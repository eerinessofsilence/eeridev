services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: eeridev-app
    restart: unless-stopped
    environment:
      NODE_ENV: production
      HOSTNAME: 0.0.0.0
      PORT: 3000
    ports:
      - "127.0.0.1:3000:3000"
